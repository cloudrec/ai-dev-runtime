# Owner OS event 8061 — result

1. Red condition 1: `owner_push` (Telegram) — `Bad Request: chat not found`.
2. Red condition 2: `same_chat_wake` (HTTP relay) unconfigured.
3. Current error/evidence: `notifications_status()` re-check (07:14:45Z) matches event's own checked_at (07:00:27Z) exactly.
4. Healthy: `cto_inbox` (available/verified); fleet-health timer active, all 5 hosts up; failed-units delta clean (ezetta-conn-sensor.service self-resolved, no new failures).
5. Changed: nothing — no safe local remediation exists for either red reason (both are owner-only external gates, previously verified with an isolated synthetic test that the notifier plumbing itself has no defect).
6. Owner action required: press Start on `@ezzetasecurity_bot` / confirm `TELEGRAM_CHAT_ID` in `configs/.env`.
